@model IEnumerable<ProjectHall4.Models.Children>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ChildrenFood)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ChildrenFoodPrice)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ChildrenFood)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ChildrenFoodPrice)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ChildrenID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ChildrenID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ChildrenID })
        </td>
    </tr>
}

</table>
