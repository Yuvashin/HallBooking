@model IEnumerable<ProjectHall4.Models.Adult>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.CateringType)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.CateringPrice)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.CateringType)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.CateringPrice)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.AdultID }) |
            @Html.ActionLink("Details", "Details", new { id=item.AdultID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.AdultID })
        </td>
    </tr>
}

</table>
