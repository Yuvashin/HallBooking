@model IEnumerable<ProjectHall4.Models.TotalPrices2>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.UserDecor.Email)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Venue.VenueName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.UserDecor.Email)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Venue.VenueName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.TotalPrices2ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.TotalPrices2ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.TotalPrices2ID })
        </td>
    </tr>
}

</table>
